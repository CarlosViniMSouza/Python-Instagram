# pip install phonenumbers

import phonenumbers

form phonenumbers import carrier

phome_num = phonenumbers.parse('<Write_with_country_code>')

print(geocoder.description_for_number(phone_num, 'pt'))

print(carrier.name_of_number(phome_num, 'pt'))

print(timezone.time_zones_for_number(phome_num))
